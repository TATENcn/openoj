name: Documentation governance

on:
  push:
  pull_request:
  merge_group:
    types:
      - checks_requested

permissions:
  contents: read

jobs:
  docs:
    name: Documentation and skill structure
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - name: Install repository checker dependencies
        run: |
          sudo apt-get update
          sudo apt-get install --yes jq ripgrep
      - name: Validate documentation governance
        run: bash scripts/check-docs.sh
      - name: Validate runtime image source locks
        run: bash infra/runtime-images/algorithm-c/check.sh
